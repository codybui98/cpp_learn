#include<iostream>
using namespace std;

int main() {
    int a, b;
    cout << "Hay nhap he so a,b: ";
    cin >> a >> b;
    cout << "Nghiem cua ptr " << a << "x +" << b << "y = 0 la: " << "x = " << (float)-b / a;
    return 0;
}